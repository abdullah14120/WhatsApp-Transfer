pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}

// إزالة المسافة البيضاء لتجنب فشل CLI Task Parsing
rootProject.name = "WhatsAppTransfer"
include ':app'
